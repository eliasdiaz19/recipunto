import { redirect } from 'next/navigation'

export default function AppPage() {
  // Por ahora redirigimos a la página principal
  // Cuando tengas la app completa, aquí irá el dashboard principal
  redirect('/')
}
